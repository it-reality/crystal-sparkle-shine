# 📋 ЧЕТКАЯ ИНСТРУКЦИЯ ПО ДЕПЛОЮ НА VERCEL

## 🎯 Вариант 1: Автоматический деплой через GitHub (САМЫЙ ПРОСТОЙ)

### Если ваш репозиторий УЖЕ подключен к Vercel:

1. **Откройте терминал в папке проекта**
2. **Выполните команды:**
   ```bash
   git add .
   git commit -m "Deploy update"
   git push
   ```
3. **Готово!** Vercel автоматически задеплоит проект
4. **Проверьте:** https://vercel.com/dashboard

---

## 🔧 Вариант 2: Подключение репозитория к Vercel (если еще не подключен)

### Шаг 1: Откройте Vercel
1. Перейдите на https://vercel.com
2. Войдите через GitHub (если еще не вошли)

### Шаг 2: Импортируйте проект
1. Нажмите кнопку **"Add New Project"** (или **"New Project"**)
2. Выберите репозиторий: **it-reality/crystal-sparkle-shine**
3. Нажмите **"Import"**

### Шаг 3: Настройки проекта
Vercel автоматически определит настройки из `vercel.json`:
- **Framework Preset:** Vite
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install`

**Просто нажмите "Deploy"!**

### Шаг 4: Готово!
После деплоя вы получите ссылку вида: `https://crystal-sparkle-shine.vercel.app`

**Теперь каждый `git push` будет автоматически деплоить проект!**

---

## 💻 Вариант 3: Деплой через Vercel CLI

### Шаг 1: Авторизация
1. Откройте терминал в папке проекта
2. Выполните:
   ```bash
   vercel login
   ```
3. Откроется браузер - войдите в аккаунт Vercel
4. Вернитесь в терминал - должно появиться сообщение об успешной авторизации

### Шаг 2: Привязка к проекту
Выполните:
```bash
vercel link
```

Вам нужно будет выбрать:
1. **Scope** - вашу команду/аккаунт
2. **Project** - выберите существующий проект из списка
   - Или введите название проекта вручную
   - Или выберите "Link to existing project" и введите Project ID

### Шаг 3: Сборка проекта
```bash
npm run build
```

### Шаг 4: Деплой
```bash
# Деплой в production
vercel --prod
```

Или для preview (тестовый):
```bash
vercel
```

---

## 🔑 Вариант 4: Деплой с токеном (без браузера)

### Шаг 1: Получите токен
1. Откройте https://vercel.com/account/tokens
2. Нажмите **"Create Token"**
3. Введите название (например: "Deploy Token")
4. Скопируйте токен (он показывается только один раз!)

### Шаг 2: Деплой с токеном
```bash
# Сначала соберите проект
npm run build

# Деплой с токеном
vercel --token=ВАШ_ТОКЕН --prod
```

Или если проект уже привязан:
```bash
vercel --token=ВАШ_ТОКЕН --prod
```

---

## ✅ Проверка деплоя

После деплоя:

1. **Откройте:** https://vercel.com/dashboard
2. **Найдите ваш проект**
3. **Проверьте статус** последнего деплоя:
   - ✅ **Ready** - успешно задеплоено
   - ⏳ **Building** - идет сборка
   - ❌ **Error** - ошибка (смотрите логи)

4. **Откройте сайт** по ссылке из Vercel

---

## 🐛 Решение проблем

### Проблема: "No existing credentials found"
**Решение:**
```bash
vercel login
```

### Проблема: "Project not found"
**Решение:**
```bash
vercel link
# Выберите существующий проект или создайте новый
```

### Проблема: Ошибка сборки
**Решение:**
1. Проверьте логи в Vercel Dashboard
2. Убедитесь, что все зависимости в `package.json`
3. Проверьте, что `vercel.json` настроен правильно

### Проблема: SPA routing не работает
**Решение:**
Убедитесь, что в `vercel.json` есть:
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

---

## 📝 Быстрая шпаргалка

```bash
# 1. Авторизация
vercel login

# 2. Привязка к проекту
vercel link

# 3. Сборка
npm run build

# 4. Деплой
vercel --prod
```

Или просто (если репозиторий подключен):
```bash
git push
```

---

## 🎯 Рекомендация

**Используйте Вариант 1 или 2** - это самый простой и надежный способ:
- ✅ Автоматический деплой при каждом `git push`
- ✅ Не нужно каждый раз запускать команды
- ✅ История всех деплоев в одном месте
- ✅ Preview деплои для каждого коммита

**Настройте один раз - деплойте автоматически!** 🚀

