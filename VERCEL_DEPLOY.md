# 🚀 Деплой в существующий проект Vercel

## ✅ Быстрый способ (если репозиторий уже подключен)

Если ваш GitHub репозиторий уже подключен к Vercel:

```bash
# Просто запушьте изменения
git add .
git commit -m "Update project"
git push
```

Vercel автоматически задеплоит проект! 🎉

---

## 📋 Подробные инструкции

### Способ 1: Автоматический деплой через GitHub (Самый простой)

**Если репозиторий уже подключен:**

1. Внесите изменения в код
2. Закоммитьте и запушьте:
   ```bash
   git add .
   git commit -m "Your commit message"
   git push
   ```
3. Vercel автоматически обнаружит изменения и задеплоит
4. Проверьте статус на https://vercel.com/dashboard

**Если репозиторий НЕ подключен:**

1. Откройте https://vercel.com/new
2. Импортируйте репозиторий `it-reality/crystal-sparkle-shine`
3. Vercel автоматически определит настройки из `vercel.json`
4. Нажмите "Deploy"
5. Теперь каждый `git push` будет автоматически деплоить проект

---

### Способ 2: Через Vercel CLI

#### Шаг 1: Авторизация

```bash
vercel login
```

Откроется браузер для авторизации.

#### Шаг 2: Привязка к существующему проекту

```bash
vercel link
```

Вам нужно будет:
1. Выбрать вашу команду (Scope/Team)
2. Выбрать существующий проект из списка
   - Или ввести название проекта вручную
   - Или выбрать "Link to existing project" и ввести Project ID

#### Шаг 3: Деплой

```bash
# Деплой в preview (для тестирования)
vercel

# Деплой в production
vercel --prod
```

#### Использование скрипта (Windows PowerShell)

```powershell
.\deploy-vercel.ps1
```

---

### Способ 3: Через веб-интерфейс

1. Откройте https://vercel.com/dashboard
2. Найдите ваш проект
3. Нажмите на три точки (⋯) рядом с проектом
4. Выберите "Redeploy"
5. Выберите нужный коммит из истории
6. Нажмите "Redeploy"

---

### Способ 4: Принудительный деплой (без привязки)

Если нужно задеплоить без привязки к проекту:

```bash
# С указанием проекта и команды
vercel --prod --yes --scope=YOUR_TEAM_NAME --project=YOUR_PROJECT_NAME
```

---

## ⚙️ Конфигурация проекта

Файл `vercel.json` уже настроен:

```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

Это обеспечивает:
- ✅ Правильную сборку проекта
- ✅ SPA routing (все маршруты ведут на index.html)
- ✅ Кэширование статических файлов

---

## 🔐 Переменные окружения

Если нужны переменные окружения:

### Через CLI:
```bash
# Добавить переменную
vercel env add VARIABLE_NAME production

# Посмотреть переменные
vercel env ls
```

### Через веб-интерфейс:
1. Откройте проект на https://vercel.com/dashboard
2. Settings → Environment Variables
3. Добавьте нужные переменные
4. Выберите окружения (Production, Preview, Development)

**Важно:** Для Vite используйте префикс `VITE_`:
```env
VITE_SUPABASE_URL=your_url
VITE_SUPABASE_ANON_KEY=your_key
```

---

## 📊 Полезные команды

```bash
# Проверить статус авторизации
vercel whoami

# Посмотреть список деплоев
vercel ls

# Посмотреть логи последнего деплоя
vercel logs

# Открыть проект в браузере
vercel open

# Посмотреть информацию о проекте
vercel inspect

# Удалить проект
vercel remove
```

---

## 🔍 Проверка деплоя

После деплоя проверьте:

1. **Статус деплоя:**
   - Откройте https://vercel.com/dashboard
   - Найдите ваш проект
   - Проверьте статус последнего деплоя

2. **Логи:**
   - Нажмите на деплой
   - Откройте вкладку "Logs" для просмотра логов сборки

3. **Домен:**
   - После успешного деплоя вы получите URL вида:
   - `https://your-project.vercel.app`
   - Или ваш кастомный домен (если настроен)

---

## ❓ Решение проблем

### Ошибка: "No existing credentials found"
```bash
vercel login
```

### Ошибка: "Project not found"
```bash
vercel link
# Выберите существующий проект или создайте новый
```

### Ошибка сборки
- Проверьте логи в веб-интерфейсе Vercel
- Убедитесь, что `package.json` содержит все зависимости
- Проверьте, что `vercel.json` настроен правильно

### SPA routing не работает
Убедитесь, что в `vercel.json` есть секция `rewrites`:
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

---

## 🎯 Рекомендация

**Используйте автоматический деплой через GitHub** - это самый простой и надежный способ:

1. Подключите репозиторий к Vercel (один раз)
2. Просто делайте `git push` - деплой происходит автоматически
3. Каждый коммит создает preview деплой
4. Деплой в main ветку = production деплой

Удачи! 🚀
