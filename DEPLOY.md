# Инструкция по деплою сайта

Проект успешно собран и готов к деплою. Ниже приведены инструкции для различных платформ хостинга.

## 📦 Собранный проект

Проект уже собран в папку `dist/`. Вы можете загрузить содержимое этой папки на любой статический хостинг.

## 🚀 Варианты деплоя

### 1. Vercel (Рекомендуется)

**Способ 1: Через веб-интерфейс**
1. Зайдите на [vercel.com](https://vercel.com) и войдите через GitHub
2. Нажмите "Add New Project"
3. Импортируйте ваш репозиторий
4. Vercel автоматически определит настройки из `vercel.json`
5. Нажмите "Deploy"

**Способ 2: Через CLI**
```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте проект
vercel

# Для продакшена
vercel --prod
```

### 2. Netlify

**Способ 1: Через веб-интерфейс**
1. Зайдите на [netlify.com](https://netlify.com) и войдите
2. Перетащите папку `dist/` в область "Deploy manually"
3. Или подключите репозиторий GitHub - Netlify автоматически использует `netlify.toml`

**Способ 2: Через CLI**
```bash
# Установите Netlify CLI
npm i -g netlify-cli

# Войдите в аккаунт
netlify login

# Задеплойте проект
netlify deploy --prod --dir=dist
```

### 3. GitHub Pages

1. Установите пакет для деплоя:
```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:
```json
"scripts": {
  "deploy": "gh-pages -d dist"
}
```

3. Задеплойте:
```bash
npm run deploy
```

4. В настройках репозитория GitHub включите GitHub Pages (Settings > Pages)

### 4. Cloudflare Pages

1. Зайдите на [dash.cloudflare.com](https://dash.cloudflare.com)
2. Выберите Pages > Create a project
3. Подключите репозиторий GitHub
4. Настройки:
   - Build command: `npm run build`
   - Build output directory: `dist`
   - Framework preset: Vite

### 5. Обычный хостинг (FTP/SSH)

1. Загрузите все файлы из папки `dist/` на ваш хостинг
2. Убедитесь, что сервер настроен на обслуживание `index.html` для всех маршрутов (SPA routing)
3. Для Apache создайте файл `.htaccess` в корне:
```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

4. Для Nginx добавьте в конфигурацию:
```nginx
location / {
  try_files $uri $uri/ /index.html;
}
```

## 🔧 Пересборка проекта

Если нужно пересобрать проект после изменений:

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

## 📝 Важные замечания

- ✅ Проект использует React Router, поэтому нужна настройка для SPA routing
- ✅ Все конфигурационные файлы уже созданы для Vercel и Netlify
- ✅ Изображения оптимизированы автоматически
- ✅ Файлы сжаты (gzip и brotli)

## 🌐 Переменные окружения

Если используете переменные окружения (например, для Supabase), создайте файл `.env.production`:

```env
VITE_SUPABASE_URL=your_url
VITE_SUPABASE_ANON_KEY=your_key
```

И добавьте префикс `VITE_` для всех переменных, которые должны быть доступны в клиентском коде.

## ✅ Проверка после деплоя

После деплоя проверьте:
- [ ] Главная страница загружается
- [ ] Навигация работает
- [ ] Все маршруты доступны
- [ ] Изображения загружаются
- [ ] Формы работают (если есть)

Удачи с деплоем! 🎉

